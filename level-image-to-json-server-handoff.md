# 手绘关卡图片转 JSON：服务端交接文档

> 文档状态：待服务端实施
>
> 协议版本：Level API v1
>
> 配套协议：[level-image-to-json-api.md](./level-image-to-json-api.md)
>
> 最后更新：2026-09-07

## 1. 交接目标

服务端接收用户拍摄的手绘关卡照片，将纸张区域拉正，并输出：

- 拉正后的原始照片，供 Unity 直接作为游戏背景；
- 与背景图片严格共用同一像素坐标系的关卡 JSON；
- 位于手绘横线位置的不可见平台碰撞线；
- 玩家出生点、终点区域和识别置信度；
- 可玩性诊断、失败原因和供排查使用的检测叠加图。

本功能的目标是“忠实转换用户画的图”，不是自动设计或美化关卡。服务端不得为了让关卡可玩而压缩、移动、延长、补齐或重新排列用户画出的平台。若草图不可识别或不可玩，应明确返回问题，让用户重拍或修改草图。

## 2. 已确认的产品原则

1. 拉正后的照片直接作为游戏背景，不重新绘制可见平台。
2. JSON 中的平台仅用于生成不可见碰撞体。
3. 平台坐标必须落在照片中对应墨迹横线的位置。
4. 所有几何坐标使用拉正图片的原始像素，不使用逻辑宽度、屏幕坐标或 Unity 世界坐标。
5. 识别算法不得以“归一化布局”为理由改变各线段之间的相对位置。
6. 可玩性分析只能诊断，不得反向修改识别结果。
7. 测试阶段允许客户端提交可调的跳跃能力参数；该参数只参与可玩性判断。
8. 低置信度结果进入复核状态，不得伪装成成功结果。

## 3. 当前项目已经具备的能力

### 3.1 Unity 运行时

当前 Unity 项目已经具备静态关卡 JSON 的基本数据结构和加载能力：

- `C1LevelDefinition`：画布尺寸、平台、出生点和终点区域；
- `C1LevelLoader`：从 `Resources/C1Levels/level1.json` 加载固定样例；
- 运行时可以按 JSON 坐标创建平台碰撞体；
- 已有测试阶段的角色跳跃参数调节入口；
- WebGL 已接入移动端控制与浏览器拍照入口。

### 3.2 本地 OpenCV 工具

`Tools/PaperLevel/rectify_paper.py` 已能完成基础纸张轮廓检测和透视拉正。

`Tools/PaperLevel/tests/test_level_alignment.py` 可以检查既有 JSON 的平台端点是否接近图片墨迹，用于回归验证。

这两个工具目前都是离线辅助工具，不是可供 Unity 调用的服务器能力。

### 3.3 当前样例 JSON 的来源

当前 `level1.json` 不是由完整的自动识别流水线生成，而是经过以下过程得到：

1. 对照片进行视觉分析，获得候选平台；
2. 使用 OpenCV 拉正纸张；
3. 将候选位置换算为拉正图片像素坐标；
4. 人工调整平台端点；
5. 使用墨迹对齐测试进行校验；
6. 将结果作为静态资源写入项目。

因此，目前不能把任意手机照片自动转换为可加载的关卡 JSON。

## 4. 当前缺失项

### 4.1 服务端缺失

- `/v1/levels` 上传、查询和调试接口尚未实现；
- 异步任务队列、状态持久化和任务幂等尚未实现；
- 上传文件的真实格式校验、EXIF 方向处理和尺寸限制尚未实现；
- 纸张检测失败、多张纸、严重遮挡等异常分类尚未实现；
- 横线检测、碎线合并、噪声过滤和端点精修尚未实现；
- 终点旗帜候选检测和语义复核尚未实现；
- 出生点推断规则尚未服务化；
- OpenCV 与 LLM 的协作、结构化输出校验和降级策略尚未实现；
- 识别置信度、对齐评分和人工复核状态尚未实现；
- 基于角色能力参数的可玩性分析尚未实现；
- 拉正图、遮罩图、叠加图、JSON 和分析报告的产物存储尚未实现；
- 测试样本集、标注真值、精度指标和回归流水线尚未建立；
- 鉴权、限流、CORS、HTTPS、日志脱敏和图片生命周期策略尚未确定。

### 4.2 Unity 客户端缺失

- 目前只读取 `Resources` 内的固定 JSON，尚未调用 `/v1/levels`；
- 尚未实现图片上传、`jobId` 保存、轮询、取消和失败重试；
- 尚未实现从 URL 下载拉正背景图并在运行时创建纹理；
- 当前 `backgroundImage` 表示本地 Resource 路径，不能直接表达服务器产物地址；
- 当前 DTO 未包含协议版本、坐标系、置信度、分析结果和状态；
- 尚未针对 `needs_review` 和 `needs_fix` 提供用户提示与重拍入口；
- 尚未提供“识别线与照片墨迹对齐”的移动端复核界面；
- 浏览器端允许的文件大小需要与服务端最终限制保持一致；
- WebGL 刷新后的任务恢复、缓存与过期处理尚未实现。

### 4.3 协作层缺失

- 服务端与 Unity 尚未共享固定的 JSON Schema 和契约测试；
- 角色尺寸、跳跃高度、水平移动能力的像素空间定义尚未统一；
- 尚未定义服务端算法版本变化时的兼容与回滚方式；
- 尚未确定人工复核工具及其责任人；
- 尚未约定生产环境对象存储地址、任务保留时间和隐私删除机制。

## 5. 推荐总体架构

```text
手机拍照 / 选择图片
        │
        ▼
POST /v1/levels
        │
        ▼
上传校验与幂等任务创建
        │
        ▼
异步图像处理 Worker
  ├─ EXIF 与纸张拉正
  ├─ OpenCV 平台候选检测
  ├─ OpenCV 旗帜候选检测
  ├─ LLM 候选语义复核
  ├─ 像素坐标与置信度生成
  └─ 可玩性诊断
        │
        ▼
对象存储 + 任务数据库
        │
        ▼
GET /v1/levels/{jobId}
        │
        ▼
Unity 下载拉正图和 level.json
        │
        ▼
照片作为背景 + JSON 创建不可见碰撞体
```

建议复用角色图片服务的异步任务模型，但关卡任务使用独立的 `level_` 前缀、状态含义和契约类型。

## 6. OpenCV 与 LLM 的职责边界

### 6.1 OpenCV 负责确定几何事实

OpenCV 是几何坐标的唯一权威来源，负责：

- 识别纸张四角并进行透视变换；
- 保存透视变换矩阵及输入、输出尺寸；
- 消除 EXIF 方向影响；
- 进行光照补偿、灰度化、局部阈值和形态学处理；
- 检测近似水平的墨迹线段；
- 对同一条墨迹造成的碎片进行保守合并；
- 精修横线端点和中心高度；
- 生成终点旗帜的候选区域；
- 输出检测遮罩和像素级调试叠加图；
- 计算每个候选的几何置信度与墨迹覆盖率。

所有最终平台端点必须能够追溯到拉正图片中的像素证据。

### 6.2 LLM 只负责语义复核

LLM 不得自由生成平台坐标。推荐只向 LLM 提交：

- 降采样后的拉正图片；
- OpenCV 已检测出的带编号候选区域；
- 固定 JSON Schema；
- 明确的分类任务，例如“该候选是不是终点旗帜”。

LLM 可以：

- 判断候选是平台、旗帜、纸张边缘、阴影还是其他笔迹；
- 对多个旗帜候选进行排序；
- 给出需要人工复核的原因；
- 判断照片中是否存在明显遮挡、文字干扰或多张纸。

LLM 不可以：

- 补画不存在的平台；
- 为了可玩性移动或延长线段；
- 直接输出最终平台端点；
- 在缺少像素证据时猜测旗帜位置；
- 把不确定结果标记为 `ready`。

### 6.3 降级策略

- LLM 不可用但 OpenCV 高置信度：可以完成纯几何识别；旗帜语义不确定时返回 `needs_review`。
- OpenCV 无法可靠拉正纸张：直接返回 `needs_review`，不得让 LLM 猜测透视坐标。
- OpenCV 未发现平台：返回 `needs_review`，原因是 `NO_PLATFORM_DETECTED`。
- 多个旗帜候选置信度接近：返回 `needs_review`，原因是 `AMBIGUOUS_GOAL`。
- 可可靠识别但不可玩：返回 `needs_fix`，保留忠实 JSON 并给出修改草图的建议。

## 7. 服务端处理流水线

### 7.1 上传校验

1. 按文件魔数判断 JPEG 或 PNG，不能只信任扩展名和 `Content-Type`。
2. 校验文件大小、像素尺寸和解码后的总像素数。
3. 读取并应用 EXIF 方向，然后清除方向元数据。
4. 计算原始文件 SHA-256，用于幂等任务 ID 和审计。
5. 拒绝无法解码、空文件、动画图片和超限图片。

### 7.2 纸张检测与拉正

1. 缩放副本用于快速轮廓检测，保留原图到副本的比例。
2. 使用边缘检测、轮廓面积和四边形拟合寻找纸张候选。
3. 对候选角点排序为左上、右上、右下、左下。
4. 使用原图坐标计算透视矩阵并执行高质量变换。
5. 根据纸张长边自动确定横屏方向；若方向置信度不足，进入复核状态。
6. 输出 `rectified.png`。后续所有识别都必须基于该文件。

不得先在原照片中识别横线，再把线坐标单独缩放到拉正图；图像和几何必须共享同一次透视变换。

### 7.3 平台检测

推荐基线流程：

1. 对拉正图进行灰度化和局部光照估计；
2. 使用 black-hat 或自适应阈值突出深色笔迹；
3. 使用水平结构元素进行形态学开运算；
4. 使用轮廓或概率霍夫变换生成线段候选；
5. 按角度、长度、宽高比、墨迹覆盖率过滤；
6. 对 Y 距离、角度和间隙均满足阈值的共线碎片进行保守合并；
7. 沿候选周围的像素带重新估计左右端点和中心线；
8. 将结果量化为拉正图片中的整数像素；
9. 记录每条线的置信度和检测证据。

平台可以有轻微倾斜，不应强行改成水平线。严重弯曲的笔迹在 v1 中应返回复核，而不是用一条长直线代替。

### 7.4 终点旗帜检测

终点由旗杆和旗面组成。推荐先用 OpenCV 找到“近似竖线 + 相邻封闭或三角轮廓”候选，再由 LLM对候选进行语义分类。

最终 `goalRegion` 是旗帜可触发区域的像素矩形，不要求与旗面轮廓完全一致，但必须覆盖真实旗帜且不得与远处无关区域重叠。

若旗杆连接着一条平台，平台和旗杆应作为不同几何对象处理，不能将旗杆误识别为平台。

### 7.5 出生点推断

v1 草图没有强制要求绘制出生标记，因此采用可解释的默认规则：

1. 在全部平台中寻找处于画布较低区域的候选；
2. 从中选择最靠左且能够容纳角色宽度的平台；
3. 出生点 X 位于平台左端内缩一定安全距离处；
4. 出生点 Y 位于平台中心线上方，具体角色脚底偏移由 Unity 处理；
5. 在结果中将 `playerStart.source` 标记为 `inferred`。

如果不存在可靠的起始平台，返回 `needs_review`。后续版本可以支持用户绘制明确的出生标记。

### 7.6 几何校验

服务端必须执行以下校验：

- 画布宽高为正数；
- 所有坐标位于画布范围内；
- 平台长度大于最低阈值；
- 平台不得由零长度线段组成；
- 出生点有对应的承载平台；
- 终点区域存在且尺寸合理；
- 每条平台附近必须达到最低墨迹覆盖率；
- 不得存在因重复检测生成的高度重叠平台；
- JSON 中声明的背景尺寸必须与 `rectified.png` 实际像素完全一致。

### 7.7 可玩性分析

可玩性分析接收客户端提交的像素空间角色能力参数，例如：

- 最大跳跃上升高度；
- 最大水平跨越距离；
- 角色宽高；
- 着陆容差；
- 能力配置版本。

分析器建立平台间有向可达图，并检查从出生平台到终点平台是否存在路径。分析结果只影响任务状态和提示：

- 可达且识别可靠：`ready`；
- 识别可靠但不可达：`needs_fix`；
- 识别本身不可靠：`needs_review`。

跳跃参数变化不得触发重新识别图片；后续可增加独立的重新分析接口。v1 首次处理时将参数快照写入 `analysis.json`。

## 8. 坐标一致性要求

坐标偏移最常见的原因是背景图片和 JSON 经过了不同的裁剪、缩放或 Y 轴转换。服务端和客户端必须遵守以下规则：

1. `rectified.png` 是唯一空间基准。
2. 原点固定为图片左上角，X 向右，Y 向下，单位为像素。
3. JSON 中 `canvas.width`、`canvas.height` 必须等于图片真实尺寸。
4. OpenCV、叠加图和 JSON 全部使用同一坐标，不允许中间归一化后丢失裁剪偏移。
5. Unity 只在加载阶段执行一次“图片像素 → 世界坐标”变换。
6. 背景 Sprite 与碰撞体必须使用相同的锚点、缩放和视口适配矩阵。
7. 若屏幕采用 `Aspect Fit` 产生留白，留白只属于显示层，不得写回关卡坐标。
8. Y 轴翻转只能由 Unity 的统一坐标转换函数完成，不能在多个组件中重复执行。

## 9. 状态与人工处理原则

| 状态 | 含义 | 是否提供权威 JSON | 客户端行为 |
|---|---|---:|---|
| `queued` | 已进入队列 | 否 | 继续轮询 |
| `processing` | 正在处理 | 否 | 继续轮询 |
| `ready` | 识别可靠且按当前参数可玩 | 是 | 可进入游戏 |
| `needs_fix` | 识别可靠，但草图不可玩 | 是 | 展示原因并要求改图 |
| `needs_review` | 识别存在歧义 | 否，允许提供非权威草稿 | 展示叠加图，要求重拍或复核 |
| `failed` | 系统或依赖失败 | 否 | 提供重试入口 |

`needs_fix` 与 `needs_review` 必须严格区分：前者说明“图识别清楚，但关卡设计需要修改”，后者说明“系统无法确认识别结果”。

## 10. 调试产物

每个成功进入处理阶段的任务至少保存：

- `input`：原始上传文件；
- `rectified.png`：拉正后的背景图；
- `paper-mask.png`：纸张检测结果；
- `ink-mask.png`：笔迹二值图；
- `overlay.png`：在背景上以细线和编号标注检测结果；
- `level.json`：符合 v1 协议的权威关卡数据，仅在可发布时生成；
- `analysis.json`：置信度、警告、算法版本和可玩性诊断；
- `transform.json`：原图到拉正图的透视矩阵和尺寸信息。

`overlay.png` 仅用于调试，建议使用 1～2 像素线宽，不应成为游戏背景。正式游戏不渲染平台线或调试标记。

## 11. 幂等、重试与版本

- 推荐任务 ID：`level_` + 原图字节、解析选项和算法主版本共同哈希后的前 12～20 位。
- 相同输入和相同参数默认返回既有任务，避免重复计费和重复计算。
- `force=true` 可创建新的运行实例，但必须保留输入哈希用于追踪。
- Worker 重试必须保证同一任务不会发布两套冲突结果。
- `schemaVersion` 管理传输结构兼容；`algorithmVersion` 管理识别结果变化。
- 算法升级不得静默覆盖历史任务产物。

## 12. 隐私与安全

- 上传接口必须使用 HTTPS；
- 服务端校验魔数并限制解码资源，防止图片炸弹；
- 原图可能包含桌面、人物或家庭环境，应设置明确的自动删除周期；
- 日志不得记录图片二进制、签名 URL 或完整用户标识；
- 调试页面和产物 URL 在生产环境中必须鉴权或使用短期签名；
- LLM 供应商、数据保留和训练使用策略需要在上线前完成合规确认；
- 若用户删除关卡，应同时删除原图、拉正图、JSON、分析结果和缓存副本。

## 13. 测试与验收

### 13.1 样本集

至少覆盖：

- 正拍、斜拍、旋转 90° 和透视明显的照片；
- 暗光、强反光、纸张阴影和不均匀光照；
- 黑色、蓝色和铅笔笔迹；
- 长线、短线、轻微倾斜、断续线和相邻平行线；
- 单旗帜、多旗帜候选、无旗帜；
- 纸张被手指遮挡、纸张边缘不完整和背景纹理复杂；
- 可玩、不可玩和识别不确定三类关卡。

每张样本应有人工作为真值标注：纸张四角、平台端点、终点区域和期望状态。

### 13.2 建议验收指标

- 背景尺寸与 JSON 画布尺寸一致率：100%；
- 高置信度 `ready` 样本的平台误检率和漏检率应分别统计，不得只统计总体准确率；
- 平台端点到人工标注墨迹中心线的中位误差：不超过 3 px 或短边的 0.4%，取较大值；
- 95 分位端点误差：不超过 8 px 或短边的 1%，取较大值；
- `ready` 样本不得出现坐标系整体翻转或统一平移；
- 不确定样本必须进入 `needs_review`，不得以猜测结果通过；
- 已标注不可玩样本必须返回 `needs_fix`，且 JSON 仍保持原图几何；
- 相同输入、参数和算法版本应产生字节级稳定的 JSON；
- 每个发布版本必须通过固定黄金样本回归。

具体阈值应由首批真实样本统计后校准，调整时需记录算法版本。

## 14. 推荐实施阶段

### P0：打通确定性链路

- 实现异步任务、上传校验、状态查询和产物存储；
- 服务化现有纸张拉正脚本；
- 建立 v1 JSON Schema 和契约测试；
- 使用少量固定样本验证背景与坐标完全对齐；
- Unity 实现上传、轮询、远程背景和动态 JSON 加载。

### P1：平台自动识别

- 实现 OpenCV 横线检测、端点精修和调试叠加图；
- 建立人工真值样本集与像素误差指标；
- 对低置信度结果返回 `needs_review`。

### P2：旗帜语义与可玩性

- 增加旗帜候选检测和 LLM 候选复核；
- 增加出生点推断；
- 增加像素空间可玩性分析和 `needs_fix` 状态；
- 接入测试阶段可调跳跃参数。

### P3：生产化

- 鉴权、限流、监控、告警、隐私删除和成本控制；
- 人工复核工具与失败样本回流；
- 灰度发布、算法版本对比和一键回滚。

## 15. 联调交付清单

服务端交付：

- `/v1/levels` 可访问环境；
- OpenAPI 或等价接口定义；
- 请求、状态和最终结果的 JSON Schema；
- 至少一组成功、不可玩、待复核和失败样例；
- 调试叠加图及其字段说明；
- 错误码、任务保留时间和重试说明；
- 算法版本、测试报告和已知限制。

Unity 交付：

- 上传与轮询客户端；
- 远程图片加载和缓存；
- v1 DTO 与契约测试；
- 唯一的像素到世界坐标转换实现；
- `ready`、`needs_fix`、`needs_review` 和 `failed` 的界面流程；
- 手机端拍照、进度、重试和重拍体验。

双方共同完成：

- 使用同一张黄金样本验证拉正图、叠加图、JSON 和 Unity 碰撞体重合；
- 对齐上传大小、超时、轮询间隔、CORS 和 HTTPS 配置；
- 冻结 Level API v1 后再进入生产联调。
